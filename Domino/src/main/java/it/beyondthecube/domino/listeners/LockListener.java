package it.beyondthecube.domino.listeners;

public class LockListener {
	//TODO
	/*
	@EventHandler
	public void onPlayerListener(PlayerEvent e) {
		if (e instanceof Cancellable) {
			((Cancellable) e).setCancelled(true);
		}
	}

	@EventHandler
	public void onBlockListener(BlockEvent e) {
		if (e instanceof Cancellable) {
			((Cancellable) e).setCancelled(true);
		}
	}

	@EventHandler
	public void onEntityListener(EntityEvent e) {
		if (e instanceof Cancellable) {
			((Cancellable) e).setCancelled(true);
		}
	}*/
}
